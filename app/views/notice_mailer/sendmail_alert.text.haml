= @user.email + 'さん'

= "Date   : #{@date}"
= "Status : #{@status}"
= "Site   : #{@site.name}"
= "URL:   : #{@site.url}"
= "Diff   : " unless @diff_html.blank?
- @diff_html.each do |diff_html|
  = diff_html

上記のサイトを確認してください。
