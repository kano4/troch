.block
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Site
    .inner
      = form_for @site, :url => site_path(@site), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
