%h2
  = @site.name
%table.confirm
  %tr
    %td 現在のステータス
    %td= @site.watch_logs.last.status unless @site.watch_logs.last.blank?
  %tr
    %td URL(チェック先)
    %td
      =link_to @site.url, @site.url
  %tr
    %td 監視方法
    %td= @site.watch_method "(キーワード): #{@site.keyword}" if @site.watch_method == 'html_keyword' && !@site.keyword.blank?
  %tr
    %td ドメイン名
    %td= @site.domain_url
  %tr
    %td ドメイン有効期限
    %td= @site.domain_expired
  %tr
    %td SSLドメイン名
    %td= @site.ssl_url
  %tr
    %td SSL有効期限
    %td= @site.ssl_expired
  %tr
    %td グループ
    %td= link_to(@site.group.name, site_path(@site.group)) unless @site.group.blank?
  %tr
    %td 備考
    %td= @site.note

.wat-cf
  = link_to image_tag("web-app-theme/icons/application_edit.png") + " 編集", edit_site_path(@site), :class => "button"
  = link_to image_tag("web-app-theme/icons/cross.png") + " 削除", site_path(@site), :method => "delete", :class => "button", :confirm => "削除してよろしいですか？"
%br
= link_to '戻る', sites_path
